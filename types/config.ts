// Shared config types for the entire application
// Re-exports from lib/config/types.ts for convenience

export type {
  TypographyConfig,
  ColorConfig,
  EffectConfig,
  LayoutConfig,
  AnimationConfig,
  ConfigSnapshot,
  Preset,
  PresetCollection,
  FontLibraryEntry,
  ColorPalette,
  ContrastValidation,
} from '@/lib/config/types'

export type {
  TextTransform,
  GradientType,
  TextAlign,
  Position,
  EntranceAnimation,
  SyncStatus,
  FontCategory,
  LoadingStatus,
  PaletteCategory,
} from '@/lib/config/types'
